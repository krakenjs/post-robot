/**
 * Flowtype definitions for types
 * Generated by Flowgen from a Typescript Definition
 * Flowgen v1.14.1
 * @flow
 */

import { ZalgoPromise } from "zalgo-promise";
import type { CrossDomainWindowType } from "cross-domain-utils";
import type {
  RequestMessage,
  AckResponseMessage,
  ResponseMessage,
} from "../types";
import type { OnType, SendType } from "../../types";
declare export function handleRequest(
  source: CrossDomainWindowType,
  origin: string,
  message: RequestMessage,
  x: {
    on: OnType,
    send: SendType,
    ...
  }
): ZalgoPromise<void>;
declare export function handleAck(
  source: CrossDomainWindowType,
  origin: string,
  message: AckResponseMessage
): void;
declare export function handleResponse(
  source: CrossDomainWindowType,
  origin: string,
  message: ResponseMessage
): void | ZalgoPromise<void>;
